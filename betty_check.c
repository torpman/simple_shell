#include <stdio.h>

/**
* main - The program entry point
*
* Return: return 0 on success
*/

int main(void)
{
    int a, b, sum;

    a = 33;
    b = 45;
    sum = a + b;

    printf("The sum of %d and %d is = %d", a, b, sum);

    return (0)
}